# Optional. Points the app at a different Swipelux base URL (defaults to the sandbox).
# The API key and customer are NOT read from env — paste the key in-app via "Go live".
# VITE_API_URL=https://platform.sbx.swipelux.com
